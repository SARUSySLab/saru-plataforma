# Decisões de produto

Uma linha por decisão: data, decisão, motivo, alternativa descartada, status. Só decisão presa
ao código: regra de leitor, issue, medição, ADR de código. Decisão de empresa mora em
`vitormtt/saru`, `docs/decisions.md`.

| Data | Decisão | Motivo | Alternativa descartada | Status |
|---|---|---|---|---|
| 2026-09-14 | `vitormtt/saru` volta a ser o repositório da empresa; este repositório fica só com código de produto e o documento preso ao código. Revoga aqui a linha de repositório único e a de física em `docs/empresa/fisica/`; `docs/empresa/` sai deste repositório | Produto novo vira pasta nova sem arrastar a empresa junto (Vitor, 2026-09-14). Registro em https://github.com/vitormtt/saru/pull/17 | Repositório único com a empresa em `docs/empresa/` | aprovada |
| 2026-09-14 | `CLAUDE.md` da plataforma (até 40 linhas) entra na branch `port/saru-docs`, dentro do PR 10, antes do merge | Um PR a menos; o contrato nasce junto com o porte | PR novo em `main` depois do 10 | aprovada |
| 2026-09-14 | As nove issues abertas da PoC (#24, #21, #7, #4, #3, #10, #2, #6, #19) são portadas para `saru-plataforma` e fechadas aqui, na ordem acordada; a PoC não recebe mais issue | Repositório único; a PoC está congelada | Fechar na PoC e espelhar aqui | aprovada |
| 2026-09-14 | Issues da PoC portadas para `saru-plataforma`: #24→#25, #21→#26, #7→#27, #4→#28, #3→#29, #10→#30, #2→#31, #6→#32, #19→#33; mesmo texto, rótulos PIL, tipo e prio | Repositório único; PoC congelada | Renumerar por família | aprovada |
| 2026-09-14 | Regra de leitura do `.pds` (E-RN-02): registro da tabela válido pelo conteúdo, tamanho da amostra deduzido da distância entre blocos (1, 2, 4 ou 8 B), teto de 90% de pares fechados; medido em 71 arquivos, 26 abriam e 27 abrem | Campos 56 e 60 não são checksum no 992.1; nenhum campo declara o tamanho da amostra | Manter checagem por `índice + 1`; chutar 4 B | aprovada (PR 34, roteiro conferido por Vitor) |
| 2026-09-14 | Regra de leitura do `.pid` do F3 (E-RN-02): corpo intercalado por tick de 10 ms, tick 1 é marcador de bloco (nulo nos canais de 100 Hz), leitor converte só o que o cabeçalho declara e o perfil `pi_pid` carrega fator e offset medidos contra o `.dat` do Pi Toolbox em 21 a 23 sessões (distância 0,01 m, acelerador 0,001, freio 19,9645 kPa e -4132,7 kPa) | Layout contíguo de 2026-08-29 embaralhava todos os canais; fatores idênticos em todas as sessões com resíduo abaixo de 1e-3 | Manter layout contíguo; inventar fator sem medição | proposta no PR 35, a aprovar por Vitor |
| 2026-09-14 | `Acc Long` e `Acc Lat` do `.pid` ficam fora do vocabulário canônico: o ganho é o mesmo em 23 sessões (-0,02586 G por contagem) mas o zero muda por sessão (12,85 a 13,18 G, até 1,6 m/s² de erro contra o limiar de -3,5 m/s²) | Uma constante única erraria a frenagem | Mapear com o offset mediano | pendente: Vitor escolhe entre zero por sessão (carro parado, Speed = 0) ou offset mediano |
| 2026-09-14 | `make test` roda com `SARU_EXIGIR_BANCO=1`: teste pulado por falta de PostgreSQL reprova a suíte; regra de uma linha no `CLAUDE.md` | No PR 35 um teste de banco pulado foi reportado como verde e só falhou com o banco de pé | Confiar no resumo do pytest, que não mostra motivo de pulo por padrão | proposta no PR 47, a aprovar por Vitor |
| 2026-09-14 | Auditoria de cobertura de dado dos leitores exceto `.pds`/`.pid` (E-RN-02): `.ld` é o pior coberto (6,3% dos canais reais mapeados em `seeds/aliases.yaml`, mais falha de `ler()` em canal real), `.xrk` é o maior volume afetado (687 arquivos únicos, 22% de cobertura), e 485 `.dat` do Drive não entraram no inventário curado, dos quais só 15% são o LISTHEAD do Pi; ordem de ataque proposta: `.ld` (#38), `.xrk` (#37), `.dat` (#39), `.mf4` (#40), `.dat` LISTHEAD (#42), `.vbo` (#41) | Vitor pediu cobertura quase total sem descartar canal raro; medição contra amostra real do acervo, não suposição | Medir só um formato por vez sem tabela consolidada | proposta em `docs/cobertura-de-dados-resumo.md`, a priorizar por Vitor |
| 2026-09-14 | Ordem de ataque da cobertura de dados: #45 (medir tempo e memória de `ler()` em todos os leitores) primeiro, depois `.ld` (#38), `.xrk` (#37), `.dat` (#39 e #42), `.mf4` (#40), `.vbo` (#41) | Medir onde está a lentidão antes de mexer em leitor; depois pior cobertura e maior volume | Ordem proposta pelo agente, sem #45 na frente | aprovada por Vitor |
| 2026-09-14 | Leitor `.pid` lê a contagem de cada canal numa única indexação vetorizada do layout por tick; do commit `66d9935` do Antigravity nada mais entra: leitura em pedaços de 64 KB, `TransformacaoLinear`, pacote `src/telemetria/` e leitura de amostra do `.pds` ficam de fora | Medido nos 25 `.pid` do acervo: mesma saída célula a célula, `ler()` 0,947 s para 0,576 s somados, pico de memória igual; 64 KB deu 2,7 vezes o tempo e pico 40% maior (`docs/pi-pid-medicao.md`, seção 7; issue #45) | Portar o `cosworth_pi.py` inteiro; manter o laço por janela e por byte | proposta nos PRs #34/#35, a aprovar por Vitor |
| 2026-09-14 | `Acc Long`/`Acc Lat` do `.pid` ganham calibração de zero POR GRAVAÇÃO (migration 025, `calibracao_canal_gravado`), medida pelo pipeline de ingestão contra o próprio arquivo (média da contagem crua com `Speed` = 0 sustentado); sem trecho parado, o canal fica fora do inventário daquela gravação. `mapeamento_canal` continua por perfil, sem offset estático no `aliases.yaml` (issue #36) | O offset varia 12,85 a 13,18 G entre sessões (issue #26); a tabela de mapeamento é por perfil e não cabe um número por arquivo | Offset fixo no `aliases.yaml`; já tentado, documentado como errado na issue | proposta no PR 46, a aprovar por Vitor; validado em 21 de 23 pares `.pid`×`.dat`, erro máximo 0,71 m/s² (mediana 0,33), abaixo do limiar de frenagem mas ACIMA do critério de 0,05 m/s² da issue; esse critério foi escrito sem medição e fica abaixo da resolução do sensor (0,2536 m/s²/contagem); ver `docs/pi-pid-medicao.md` seção 7 |
| 2026-09-14 | Leitor `.pid` lê a contagem de cada canal numa única indexação vetorizada do layout por tick; do commit `66d9935` do Antigravity nada mais entra: leitura em pedaços de 64 KB, `TransformacaoLinear`, pacote `src/telemetria/` e leitura de amostra do `.pds` ficam de fora | Medido nos 25 `.pid` do acervo: mesma saída célula a célula, `ler()` 0,947 s para 0,576 s somados, pico de memória igual; 64 KB deu 2,7 vezes o tempo e pico 40% maior (`docs/pi-pid-medicao.md`, seção Desempenho; issue #45) | Portar o `cosworth_pi.py` inteiro; manter o laço por janela e por byte | proposta nos PRs #34/#35, a aprovar por Vitor |
